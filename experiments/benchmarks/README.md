# Benchmark Harnesses

Each subdirectory captures a workload (e.g., tiling, epilogue fusion) with:
- Baseline IR and optimized IR versions.
- Runner scripts or notebooks.
- Instructions for reproducing profiler captures stored in `results/`.
