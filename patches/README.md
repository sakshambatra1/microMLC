# Vendor Patch Queue

Drop any patches you need to apply to `third_party/stablehlo` (or other upstream projects) in this directory.  Provide short descriptions in the file headers so collaborators know why each patch exists and when it can be dropped.
